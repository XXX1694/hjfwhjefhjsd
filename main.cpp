#include <iostream>
#include <string>

using namespace std;

void myFunc(String in){
    for(int i = 0 ; i < in.length(); i++){

    }
}

int main() {
    string a;
    getline(cin, a);
    myFunc(a);
    return 0;
}
